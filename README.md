# 3D-renderer
a simple 3d renderer that runs on the terminal.
includes the function "render" that receives a set of points and the lines connecting those points and draws that to the variable "screen"
