# 3D-renderer
a simple 3d renderer that runs on the terminal.
includes the function "render" that receives a set of points, the lines connecting those points and the textures to draw on said lines and draws that to the variable "screen"
